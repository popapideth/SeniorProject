Place your trainer video file as `trainer.mp4` in this folder so the web UI can load it at /static/trainer.mp4

Example:\project\static\trainer.mp4

If you prefer another filename, update the `src` attribute of the <video id="trainer_vid"> element in templates/index.html accordingly.